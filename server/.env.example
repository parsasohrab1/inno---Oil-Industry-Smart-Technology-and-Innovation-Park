# پورت سرور
PORT=8787
# مبدأ مجاز CORS (چند مبدأ با کاما جدا شود)
CORS_ORIGIN=http://localhost:5173
# مسیر فایل پایگاه داده SQLite
DB_PATH=./data/oipms.db
# کلید امضای JWT — در تولید حتماً تغییر دهید
JWT_SECRET=change-this-in-production
JWT_TTL=7d
